import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Art Vault - Art Gallery | Products",
  description: "Check Out Our Available Paintings",
};

export default function ProductsPage(){
    return (
        <p>Página de Produtos</p>

    );
}